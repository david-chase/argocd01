apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jupyterhub               # App name
  namespace: argocd
  labels:
    appversion: "v4.3.2"     # Version of the app 
spec:
  project: default
  source:
    repoURL: https://github.com/david-chase/argocd01
    targetRevision: HEAD
    path: jupyterhub-4.3.2             # This folder holds the kustomization.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: jupyterhub          # Namespace to deploy to
  syncPolicy:
    automated:
      prune: true
      selfHeal: true                # Auto-sync?
    syncOptions:
      - CreateNamespace=true
  ignoreDifferences:
    - group: cilium.io
      kind: CiliumIdentity