apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kyverno               # App name
  namespace: argocd
  labels:
    appversion: "v3.6.1"     # Version of the app 
spec:
  project: default
  source:
    repoURL: https://github.com/david-chase/argocd01
    targetRevision: HEAD
    path: kyverno-3.6.1             # This folder holds the kustomization.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kyverno          # Namespace to deploy to
  syncPolicy:
    automated:
      prune: true
      selfHeal: true                # Auto-sync?
    syncOptions:
      - CreateNamespace=true
  ignoreDifferences:
    - group: cilium.io
      kind: CiliumIdentity