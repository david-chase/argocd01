# Values.yaml for retailstore app
# This is manual, 1 replica
metadata:
  labels:
    tag: manual

deployments:
  assets:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  carts:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  cartsdynamodb:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  catalog:
    spec:
      replicas: 0
    resources:
      requests:
        cpu: 1000m

  checkout:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  checkoutredis:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  orders:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  ui:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

statefulsets:
  catalogmysql:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  orderspostgresql:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m

  ordersrabbitmq:
    spec:
      replicas: 1
    resources:
      requests:
        cpu: 1000m
