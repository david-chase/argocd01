apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-api-crds               # App name
  namespace: argocd
  labels:
    appversion: "v0.9"     # Version of the app 
spec:
  project: default
  source:
    repoURL: https://github.com/david-chase/argocd01
    targetRevision: HEAD
    path: gateway-api-crds             # This folder holds the kustomization.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system          # Namespace to deploy to
  syncPolicy:
    automated:
      prune: true
      selfHeal: true                # Auto-sync?
    syncOptions:
      - CreateNamespace=true
  ignoreDifferences:
    - group: cilium.io
      kind: CiliumIdentity